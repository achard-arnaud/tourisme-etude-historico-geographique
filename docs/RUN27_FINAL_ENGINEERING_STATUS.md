# Run 27 — final engineering status

## Ready

- two-stage `return_to` resolution is executable: marker first, research fallback second;
- required unresolved returns block final export;
- five legacy required pre-1948 returns missing markers have persisted research closures with two independent qualified source families each;
- Run27 from-scratch packet universe is rebuilt from structured evidence only;
- paragraph repair loop and exhaustive dispositions are active contracts;
- Run27 fresh renderer supports `--run 27` without overwriting Run26;
- dual-corpus final closure gate is `python scripts/close_run27_final.py --project all`;
- final semantic execution request is `docs/RUN27_FINAL_AGENT_REQUEST.md`;
- CI publishes `run27-final-from-scratch-inputs-*` with both packet sets and `RUN27_FINAL_RUN_REQUEST.json`.

## Evidence universe at final preflight

- pre-1948: 16 arc packets, 84 eligible claims;
- post-1948: 13 arc packets, 98 eligible claims;
- previous reader prose loaded: false for both.

## Deliberately not claimed complete

The new Run27 manuscripts, review ledgers, coverage reports and DOCX outputs are not generated by deterministic CI. They require the semantic writer/reviewer pass. PR #64 remains draft until those two manuscripts are produced and `close_run27_final.py --project all` is green, followed by the Run25-vs-Run27 claim-depth comparison.

## Run26 archive integrity

`docs/RUN26_FROM_SCRATCH_ARCHIVE_MANIFEST.json` records SHA-256 hashes for the exact six Run26 artefacts and the compact ZIP. The exact binary ZIP is present in the active execution environment; the current GitHub connector has no local-file upload parameter, so the manifest is the durable integrity record until a binary-capable push path is used. Never reconstruct Run26 from current inputs and treat it as the archived binary.

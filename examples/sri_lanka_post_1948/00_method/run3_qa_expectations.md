# Run 3 — QA expectations

The run is accepted only if deterministic project QA confirms:
- all new A/B causal claims have registered sources;
- all bridges resolve to existing claim IDs;
- no duplicate claim IDs;
- source-role and tier values remain valid;
- no development claim upgrades English policy from amplifier to master cause without new evidence.
